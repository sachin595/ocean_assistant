[pytest]
markers =
    retrieval: requires OPENAI_API_KEY and a built index
    text2sql: requires OPENAI_API_KEY and the seeded database
    web: requires OPENAI_API_KEY, a built index, and the seeded database
asyncio_mode = auto
